import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the round "N" dev-tools indicator that floats over the page in development.
  devIndicators: false,
};

export default nextConfig;
